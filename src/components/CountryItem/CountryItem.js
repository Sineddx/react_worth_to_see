import Wrapper from './CountryItem.styled'

function CountryItem({obj}) {
    return (
        <Wrapper>
            <div className="image">
                <img></img>
            </div>
        </Wrapper>

    );
}

export default CountryItem;